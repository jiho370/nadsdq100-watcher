# LIVE.md — 지금 라이브인 전략과 이유

**이 문서의 역할**: "지금 무슨 전략이고 왜 이렇게 하는지"만, 짧게. 시행착오·기각된 대안·
날짜별 세션 기록은 [`HISTORY.md`](HISTORY.md)에 있다. 체결·비용 모델의 실측 감사는
[`docs/playbook/03-method/EXECUTION.md`](../playbook/03-method/EXECUTION.md)에 있다.

> **숫자를 소유하지 않는다.** 아래 값은 전부 "코드 위치" 열이 단일 출처다. 여기 숫자와
> 실제 코드가 갈라지면 코드가 맞다 — 발견하면 이 문서를 고쳐라. (이 문서 자체가 2026-09-11
> 시점 코드를 직접 읽어서 쓴 것이다. `SYSTEM_OVERVIEW.md`의 옛 자산군 상세는 이 문서가
> 대체하며, 실제로 대조해보니 그쪽은 2026-07-22 이전 값 여러 개가 이미 낡아 있었다 —
> 한국 점수공식 전체 교체, 미·한 매도규칙 활성상태, 후보풀 크기가 전부 어긋나 있었다.)

---

## 미국 개별주 (S&P500)

**미국 주식 권장 편성(2026-09-24, HISTORY.md §17)**: 코어 50% SPMO(S&P500 모멘텀 ETF) + 위성 50% 아래 개별종목
추천 — `weekly_report.ASSETS` 미국 주식 안내에 표시. 아래는 위성(개별종목) 규칙이다.

(2026-09-24 코드 재확인 — 이전 판의 "섹터캡 2·진입필터·보유상한 8"은 전부 낡은 값이었다)

- **팩터 가중치**: `int_gp_assets×1 + rd_mktcap×2 + shareholder_yield×2` — `output/best_weights.json`
- z-score 클립: `shareholder_yield`만 ±5, 나머지 ±3 — `export_data.live_z`(백테스트도 같은 함수)
- **필터**: 종합점수 < `export_data.SCORE_FLOOR`(3.25) 제외, 100일선 아래(또는 값 없음) 제외
  (`export_data.select_by_weights`). 필터 통과 0종목이면 **신규 매수 없이 현금 대기**
  (2026-09-24부터 — 그 전엔 하이브리드 점수 전략으로 자동 전환됐다). 데이터 자체가 없을 때만
  하이브리드 폴백.
- 섹터캡: **없음**(`daily_ai_report.py` `split_by_entry(..., sector_cap=None)`)
- 진입 게이트(200일선·52주고점): 폐지 — 과열 판정은 분할 방식에만 씀(아래 실행 계획)
- **후보풀 60 → 매수 후보 상위 10 → AI 검증 → 최종 매수 10**(`REPORT_MAX_CANDIDATES`/`REPORT_POOL`/
  `REPORT_FINAL_BUY`) — 보유 상한 **10**(`US_MAX_HOLD`, "팔아야 산다")
- **매도**: 보유 `holdings.REEVAL_DAYS`(180달력일) 경과 **그리고** 그날 후보풀(60) 밖이면 정리.
  기간이 지나도 후보풀 안이면 계속 보유(고정 6개월 청산과 다른 규칙 — 검증은 아래 '검증 상태').
  트레일링 스톱·200일선 백업은 **기본 비활성**(`SELL_TRAIL=0`, `SELL_MA200_BACKUP=0`)

**왜**: 665개 대안 조합 중 1:2:2를 통계적으로 유의하게 이긴 게 없어서 유지 중(HISTORY.md §6-A,
PBO 22%·DSR 0.88로 미달 — 즉 "더 나은 게 입증 안 됨"이지 "이게 입증됨"이 아니다).
**2026-09-24 재검증(HISTORY.md §14)**: 그 665조합 탐색은 분할 미보정(미래참조)·PIT 종료종목 삭제
데이터 위에서 돌았다. 보정 후 rd_mktcap(IC t 0.23)·shareholder_yield(t 0.61)는 예측력 근거가 없고, 학습/검증
분할 재탐색의 1등은 검증 구간에서 CAGR 2.4%로 실패 — 1:2:2는 '대안이 없어서' 유지 중인 미검증 선정기다.

---

## 한국 개별주 (코스피200)

- **유니버스**: 코스피200만(전체 유니버스는 모멘텀 무의미 — universe-shrinkage 연구)
- **펀더멘탈 필터**: EPS>0(흑자) ∧ ROE(EPS/BPS)>0 — `kr_stocks.py`
- **점수** = `z(1/PER) + z(1/PBR) + z(배당수익률)` 1:1:1, 전부 ±3 클립 — "밸류×주주환원" 계열.
  z-score 모집단은 흑자 필터 통과 종목(연구 `research/kr/`는 코스피200 전체 — 알려진 차이)
- **점수상한**: `kr_stocks.SCORE_CAP`(6.0) 초과 제외(밸류트랩 구간)
- **후보풀 8 → AI 검증 → 최종 매수 5**(`KR_POOL_BUY`/`KR_FINAL_BUY`) — 보유 상한 5(`KR_MAX_HOLD`)
- **매도**: 보유 `holdings.KR_REEVAL_DAYS`(90달력일) 경과 **그리고** 팩터 순위 상위
  `KR_MAX_HOLD`(5) 밖이면 정리(2026-09-24부터 — 그 전엔 흑자·점수상한 통과 전체(~150종목)가
  풀이라 사실상 매도가 없었고, "3개월마다 상위 5 재선정"을 검증한 연구와 다른 전략이었다).
  보유종목 시세는 신규 후보 필터와 별도로 조회(적자 전환·지수 제외 종목도 매도 판정 가능).
- 참고선(실거래 아님): 보유현황 차트에 "코스피65+알고리즘35" 블렌드 표시

**왜**: 옛 ROE≥8%·PER≤40·200일선 위 필터는 저평가 구간을 스스로 걸러내는 모순이라 완화·폐기
(HISTORY.md §3). 데이터 의존성: 2025-12-27부터 KRX 로그인 필수 — `KRX_ID`/`KRX_PW` 없으면
이 섹션이 에러 없이 빈 채로 발송된다.

---

## 검증 상태 (2026-09-24 전략 검토 반영 — 저장된 결과를 읽을 때 반드시 볼 것)

2026-09-24 이전에 만들어진 `output/` 백테스트 결과에는 아래 결함이 들어 있다. 코드는 고쳤지만
**결과 파일은 재실행 전까지 옛 산식 그대로**다(이 컨테이너에선 시세 수집이 막혀 재실행 못 함).

| 결함 | 영향받는 결과 | 수정 위치 |
|---|---|---|
| 분할매수 수익을 체결분 기준으로 계산(미체결 현금 무시) · 청산 이후 추가매수가 평균단가에 섞임 | `backtest_exec_compare*`, `pbo_report_exec*`, `backtest_entry_ratio_compare*`, `backtest_disposal_compare*`, `backtest_topn_compare`, `kr_hot_split_sweep` | `backtest_exec._simulate_trade`/`_eval_event` |
| 위 결과들의 `mdd_pct`는 "최저가/평균단가-1" 평균 — 고점대비 낙폭도, 계좌 MDD도 아님 | 동일 | 새 결과는 `basket_mdd_pct`(이벤트 바스켓 가치경로 MDD) |
| PIT 백테스트가 라이브용 신선도 필터로 과거 종료 종목(인수·상장폐지) 시세를 통째로 삭제 | `build_panel_pit` 쓰는 모든 미국 연구 | `backtest_costs.build_panel_pit` |
| 과거 시총 = 수정주가 × 공시 당시 주식수 → 공시 후 분할 종목의 rd_mktcap·shareholder_yield가 분할배수만큼 부풀려짐 | 미국 팩터 가중치·필터 연구 전부(1:2:2 포함) | `fundamentals_edgar.factor_values` + `--splits-only` 수집 |
| 풀스택 검증이 라이브와 다름(클립 ±3·후보 부족 이벤트 삭제·고정 분할·고정 126일 청산) | `*_us_livestack*` | `research/us/us_full_stack_exec_validation.py` |
| 워크포워드 컷 학습에 아직 확정 안 된 6개월 수익 사용 | `kr_cap_walkforward.json` | `research/kr/kr_cap_walkforward.py` |
| 코인 CAGR·샤프를 연 252봉으로 연환산(코인은 365) | `regime_backtest_btc`, `ma_trend_btc/eth`, `circuit_breaker_*`, `eth_ma30_cost_and_wide_grid` | `backtest_regime_assets.periods_per_year` |

- **README의 CAGR 29.43%·샤프 1.02는 옛 top8·섹터캡2 설정의 결과**(같은 실험 MDD −37.4%)이며
  현행 top10·floor·100일선·섹터무제한 전략의 기대수익이 아니다. 위 데이터 결함도 들어 있다.
- 현행 조합의 저장된 PBO/DSR(topn10: PBO 60%·DSR 0.77, topn8: 66%·0.84, topn6: 21%·0.87)은
  전부 자체 게이트(PBO<50% ∧ DSR≥0.95) 미통과 — "우월성이 입증 안 됨"이지 "나쁨"의 증거는 아니다.
- DSR의 시행 수 N은 그 스크립트 안의 조합 수만 센다 — 그 전에 가중치(665)·점수하한·이동평균·
  종목 수를 반복 탐색한 부담은 빠져 있어 실제보다 관대하다. 이미 본 기간에서 새 조합을 골라
  다시 검증해도 독립 표본외 검증이 되지 않는다.
- **계좌 NAV 재검증 스냅샷(2026-09-24, HISTORY.md §13, `research/us/us_capweight_momentum_validation.py
  --with-live`)**: 2018-02~2026-09 현행 미국 전략 CAGR 18.99%·샤프 0.75 vs SPY 14.39%·0.67 vs QQQ
  19.87%·0.77 — 단 2025-09까지로 자르면 13.27%·0.57로 SPY에 진다(2026년 급등 의존, 8개 연도 중 5개
  연도 SPY 열위). 현행50:SPMO50 조합이 4개 구간 전부 SPY를 CAGR·샤프 둘 다 앞섰지만 사후 추가·
  PBO 45.9%·DSR 0.85로 게이트 미통과, 나스닥100은 어떤 후보도 일관되게 못 이겼다. 라이브 미반영.
- **재실행 순서**(PC, 네트워크 필요): `python fundamentals_edgar.py --splits-only` →
  `python -m research.us.us_full_stack_exec_validation --topn 10` → 나머지 연구. 재실행 전까지
  주력 자금 운용의 근거로 쓰지 말 것.

---

## 지수·코인·금 레짐 신호 (`market_signals.py`, 8개 핵심자산)

완전히 stateless(종가만으로 매번 상태 복원). 히스테리시스로 거짓신호 억제.

| 자산 | 추세선 | 밴드 | 확인일 | 모멘텀 | 변동성타깃 | 비고 |
|---|---|---|---|---|---|---|
| 코스피·코스닥·금·나스닥100·S&P500 | 200일 | ±1% | 3일 | 12-1 | 연15% | `equity` |
| 미국채10년(IEF) | 150일 | 0% | 3일 | (표시만) | 연15% | `bond`, 컷 판단은 레짐만 |
| 비트코인 | 120일 | ±3% | 3일 | 3개월 | 연40% | `crypto` |
| 이더리움 | 30일 | 0% | 1일 | 3개월 | 연40% | `crypto_eth`, BTC와 별도 튜닝 |

레짐×모멘텀으로 5단계(적극매수/눌림목분할매수/보유/축소검토/위험회피) 판정 — 단, **5단계
매수·매도 라벨은 무작위매매 대비 유의성이 검증된 자산(BTC·ETH)만 표시**(`RANDOM_VALIDATED`),
나머지는 원자료(가격·추세·모멘텀)만 보여준다.

**왜**: 채권은 2026-07-23 100조합 그리드 검증(추세필터만, 모멘텀·변동성타깃은 미검증).
이더리움은 2026-08-24 BTC 파라미터를 그대로 쓰던 걸 교체 — 무작위매매 대비 샤프 유의(99.8%ile)
+시대분리 2구간 모두 승 (HISTORY.md §11). 코스피·코스닥·나스닥100·S&P500 5단계 라벨은
PBO/DSR 게이트 불통과 또는 매수후유지 대비 이길 확률 50% 미만 확인.

---

## 코인전용 메일 (주말 공백 채우기)

- 토·일·월 09:07 KST, BTC+ETH 레짐 신호만(위 표 그대로) — AI 호출 없음, 종목추천·보유현황·
  자산배분 전부 없음, 비용 0
- 화~토 미장 메일의 BTC 카드는 유지(중복이지만 최소변경 원칙)

**왜**: 비트코인은 24/7인데 화~토 미장 메일이 토(금 마감 기준)~화(월 마감 기준) 사이
2.5~3일 신호 공백을 만듦(`docs/superpowers/specs/2026-08-03-coin-only-mail-design.md`).

---

## 주간 자산배분 (`weekly_report.py`, 일요일)

| 자산군 | 안정형 | 공격형 |
|---|---|---|
| 미국 주식 | 30 | 50 |
| 한국 주식 | 10 | 15 |
| 채권 | 40 | 15 |
| 금 | 10 | 10 |
| 코인(BTC 중심) | 2 | 5 |
| 현금성 | 8 | 5 |

- 리밸런싱 밴드 ±20%(`REBALANCE_BAND`) — 목표의 1.2배 초과분만 매도, 0.8배 미만이면 매수
- 방어 컷: 레짐 OFF + 12개월 수익률 음수면 목표비중 절반만 유지(컷분은 채권·현금으로)
- AI는 해설만, 비중 숫자 자체는 절대 안 바꿈(하드코딩)

**왜**: 전통 60/40 틀 + 연구 권장 범위(금 5~15%, 코인 기관가이드라인 1~5% 상한).
"금 25~30%" 같은 과최적화 배분은 폐기.

---

## 실행 계획(매수) — `entry_plan.py`, 100% 하드코딩

AI가 못 건드리는 값. 미·한 공통. 매수 규칙은 `entry_plan.tranche_targets` 한 곳에만 있고 백테스트
(`backtest_exec` `entry_live`)도 같은 함수를 쓴다.

- **현재가 전량 1회**(2026-09-24부터, HISTORY.md §17). 과열(`is_hot`: RSI≥72 또는 50일선+15%↑)은 카드 표시만.

**왜**: 예전 2분할·과열 3분할은 미체결분이 현금으로 남아(미국 42.9%) 수익을 깎았다 — 자본 회계를 고친 스윕에서
미국·한국 모두 전량 진입이 1위(낙폭 대비 수익도 우위). 분할이 채택됐던 건 미체결 현금을 수익 계산에서 뺀 회계 결함 때문.

---

## AI 검증 레이어

- 규칙이 후보 풀을 뽑고(위 자산별 후보풀 크기 참고), **sonnet**(웹검색 ≤4회)이 검증해
  `매수유지/제외` 판정 — AI는 종목을 추가할 수 없음(할루시네이션 차단), 축소만
- 서술은 **haiku**(입력에 사실이 이미 다 있어 저가 모델로 충분)
- 최종 확정은 코드(`_apply_verdicts`)가 결정론적으로 함 — AI 실패 시 지표 기반
  deterministic 리포트로 무조건 발송(발송 누락 없음)
- 유료 API는 기본 꺼짐(`report.yml` `AI_ENABLED=0`) — pregen 캐시(로컬 구독 CLI, $0)로 대체

**왜**: 초기 버전 회당 ~$0.5 비용을 pregen 사전생성 구조로 사실상 0으로 낮춤(HISTORY.md §4).

---

## 관련 문서

- [`HISTORY.md`](HISTORY.md) — 왜 이렇게 됐는지 시행착오 전체(날짜별 세션 로그)
- [`docs/playbook/03-method/EXECUTION.md`](../playbook/03-method/EXECUTION.md) — 체결·비용 모델 실측 감사
- [`KR_STRATEGY_OPTIONS.md`](KR_STRATEGY_OPTIONS.md) — 국장 미착수 전략 후보 백로그
- [`VALIDATION_PIPELINE.md`](../VALIDATION_PIPELINE.md) — PBO/DSR 채택 기준
